"""
Background "fit mode" implementations.

Each module in this package implements one visual style for filling the
space around the main source video on the output canvas (typically a
1080x1920 vertical canvas). See ``base.py`` for the shared interface and
``factory.py`` for how a mode is selected from ``settings.ini``.
"""
